{
    "name": "DEV API (beta)",
    "port": 3000,
    "hostname": "localhost",
    "routes": [{
        "endpoint": "webhooks",
        "method": "get",
        "responses": [{
            "statusCode": 200,
            "label": "A webhook endpoint"
        }],
        "default": true
    }]
}